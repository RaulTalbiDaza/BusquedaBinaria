# Binary Search

Java application that sorts a predefined array and allows the user 
to search for a number using the binary search algorithm.

## Features
- Automatic array sorting
- Binary search implementation
- Command-line user input

## Technologies
- Java

## How to run
1. Clone the repository
2. Compile: `javac Main.java`
3. Run: `java Main`
